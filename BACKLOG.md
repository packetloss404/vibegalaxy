# Backlog

## Portfolio audit backlog — 2026-07-17
_Findings from a 2026-07-17 code audit, preserved for later._

### Later / deferred
- **[med/M]** 10 game GLBs (planet, house_01-04, villager_01-05) missing from models/
  - Fix: Assets are generated by native-app/scripts/generate_models.py via Tripo API (BYO TRIPO_API_KEY), not committed; loadModel fails silently and village.js falls back to procedural (_initProcedural). Either run the generator + commit the 10 GLBs, or document the BYO-generate step. Graceful degradation exists, so this is a pipeline/docs gap, not a crash.
- **[low/S]** Hard-coded /tmp debug-log writers (wordgalaxy_debug.log, wg_scroll.log, tree_renderer_debug.log)
  - Fix: Not security-relevant (single-user macOS app, no sensitive data). Centralized in 3 helpers: AppState.debugLog (AppState.swift:50), TreeWebView scrollLog (TreeWebView.swift:5), TreeRenderer dbg (TreeRenderer.swift:55). Route them through os.Logger or gate behind a DEBUG build flag; callsites unchanged.
- **[low/M]** Debug slider/position panels built in prod DOM (hidden via display:none)
  - Fix: World/Nebula/Camera panels (main.js:474,504,536) and Cosmic Debug sliders (village.js:793) are constructed then hidden. Gate creation behind a ?debug query-param check, or remove. Minor bloat only.
- **[low/L]** No tests anywhere in repo
  - Fix: Mostly graphics/UI (hard to test), but the pure data logic is testable: WordFrequencyEngine, TreeDataCalculator, StopWords, VillageState. Add a XCTest target covering those. Real design/effort.
